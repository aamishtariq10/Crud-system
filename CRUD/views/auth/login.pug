extends ../layout
block content
 .container
   form(action = "/login" method = "post")
    .form-group
        label(for='userEmail') Email address
        input#userEm.form-control(type='email' aria-describedby='emailHelp' placeholder='Enter email' name ="userEmail")
        small#emailHelp.form-text.text-muted We&apos;ll never share your email with anyone else.
    .form-group
        label(for='userPassword') Password
        input#exampleInputPassword1.form-control(type='password' name ="userPassword" placeholder='Password')
    button.btn.btn-info(type='submit') Register
